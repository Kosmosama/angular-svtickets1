// You must use an interceptor to insert the authentication token in every
// HTTP request to the server (See here for more details), and another to set the
// base url for the server (already done in past exercises).